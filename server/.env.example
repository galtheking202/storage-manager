DATABASE_URL=postgresql://halel:changeme@localhost:5432/haleldb
JWT_SECRET=change-this-to-a-long-random-secret
PORT=3000

# First-run manager seed (optional — set once, then remove)
SEED_EMAIL=admin@example.com
SEED_PASSWORD=changeme123
SEED_NAME=מנהל ראשי
